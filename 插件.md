# K3 · DSH 动态插件开发经验与注意事项

> 面向后续会话的速查手册：在本机 DSH（DeepSeek Harness）上开发动态 Cordis 插件 / 工具箱工具的实战经验。
> 全部条目都来自真实踩坑与验证，非文档转述。配套：`PLUGIN-DEV.md`（写法契约）、`REBUILD.md`（重建流程）。

---

## 1. 心智模型

- **一切皆插件**：宿主能力是 cordis.yml 里的插件行；动态插件只是「当前进程内的临时行」，`cordis_define` 定义、`cordis_run` 激活，进程重启即消失（所以工具箱用「磁盘加载桩」把 impl 留在磁盘上）。
- **两个平面**：Host（Node 进程：文件/子进程/服务/模型 Tool）与 Client（浏览器：React Slot UI/样式/RPC 调用 Host）。跨半通信只有一条：`harness.handle`（Host 注册）+ `host.call`（Client 调用），**只允许无损 JSON**。
- **pluginId / packageId / pluginRunId**：插件是稳定实例，Package 是不可变版本，Run 是激活尝试。改代码 = 同插件 define 新 Package（或磁盘桩重跑）。批准按 Package 授权（单勾当前包，双勾未来版本），授权在技术失败后仍然有效。
- **inject 语义**：`inject: ['x']` 是硬依赖（服务缺失则插件进入 waiting 直至其出现）；可选服务一律 `ctx.get('x')` + undefined 判断。**桩架构下生效的是桩（payload）里的 inject，impl 文件里的 inject 只是文档**——两者要保持一致，别只改一边。

## 2. 工具箱 v5 架构要点

- 框架插件（tbx）Host 半 `ctx.provide('toolboxRegistry')` + `harness.handle('toolbox/tools'|'toolbox/panel'|'toolbox/plugins'|'toolbox/plugin-toggle'|'toolbox/plugin-restart'|'toolbox/plugin-toggle-all'|'toolbox/plugin-restart-all'|'toolbox/rebuild'|'toolbox/client-impl')`；Client 半是加载桩（经 `toolbox/client-impl` 拉磁盘 client.js 求值），抽屉 + Tab 栏（>8 个工具自动出过滤框）+ HTML 面板壳 + 齿轮管理视图（插件真停/真启/重跑/全部重跑/全停/全启 + 重建/补齐：直连 `dynamicCordisRunner` 服务的 stop/run，与 Cordis 面板同一注册表状态同步；含 Client 半的禁用开关）。
- **框架自举（v6）**：`doRebuild` 读磁盘 plugins.json + payload.json 经 `dynamicCordisRunner` **并行** define+run（实测全量冷重建 22 插件 ≈ 0.3s，报告含 `ms`）；幂等按 name 跳过已定义。**启停记忆**：开关/重跑/批量操作都写 `.dsh-dynamic-toolbox/toolbox-plugins.json`，重建时记录为关的只 define 不启动——「每次重建默认恢复上次开关状态」。
- **注册表 multiplex（v6.3）**：`toolboxRegistry` 是**进程级全局服务名**，但注册表按仓库根（root）分键——同一 DSH 进程内多工作区/多仓库**并行共存**（此前 v6.1 单例会因重复注册报 `service "toolboxRegistry" has been registered`）。第一个框架 `provide` 全局实例，后续框架 `attach` 自己的 root；`myRoot` 仲裁挑「尚未 attach」的仓库。工具注册归 `buildRoot`（doRebuild 的 `beginBuild/endBuild` 互斥队列 + 串行化）或 `lastRoot`。所有 RPC 按 root/cwd 参数化，抽屉用 `useSessions().current.cwd` 跟随当前工作区切换；无工具箱工作区显示空态。Client 半仍用 `data-dsh-toolbox-mounted` DOM 标记防双抽屉（首个框架被停后重跑任一框架即接管）。自举宿主会话（P2）：插件归属 `toolbox-host-<root哈希>` 垫片 agent，run 通知 no-op → 用户会话零污染。**v6.5 会话跟随**：宿主 host 的 `useSessions` 不响应 UI 切会话，抽屉经 `patch Storage.prototype.setItem` 监听 `localStorage['dsh.sessions.current']` 变化（自定义事件 `tb-session-changed`），切会话零延迟响应；`useSessions` 作兜底。**v6.5 会话/工作区查询**：新增 `toolbox/session-info` RPC（按 sessionId 经 Host sessions/sessionQuery 查 header.cwd），供抽屉按当前会话查 cwd（宿主视角 byId 记录不可靠）。
- **动态插件的 `ctx.get(name)` 不受 inject 限制**（cordis-host-runner guard.ts：`readService` 只在属性访问 `ctx.xxx` 时强制 inject 声明，`ctx.get` 透传真实上下文）——Host 半可拿到任意服务，包括 `dynamicCordisRunner`（插件清单 inventory / stop / run，面板按钮即其 @Remote 版 stopFromPanel/runHostHalf）和 `agents`（get(sessionId) 拿 live Agent）。绕 AI 注入指令（agent.steer）也能做但异步烧 token，能直连服务就别绕。
- 工具插件 = Host-only，`tryRegisterTool`（shared/host.js）注册：未注册前 500ms 快重试，成功后降 2s 慢心跳（框架重启换新注册表实例仍能自动挂上）；插件停止 → Tab 级联消失。
- **二级加载（v5）**：payload 桩（~0.9KB，只探测根目录）→ 磁盘 `loader.js`（读 impl + 求值 + 委托 apply）→ impl 文件。改 impl 和改 loader 都只需 `cordis_run` 重跑，永不重新 define。
- **嵌套 `new Function` 帧不吃外层形参**：桩→loader→impl 每层都要显式下传 `ctx/harness/console`（真机上它们是全局 builtin，但仿真/严格环境会 ReferenceError——实测踩过）。
- 面板契约：Host→Client `{ ok, html, state }`（html 走 dangerouslySetInnerHTML）；Client→Host 点击 `[data-action]`，元素自身 `data-*` 经 `fields.__el` 回传；`data-field` 表单值每次动作全量回传；回车自动触发 `data-action="query"`；**select 加 `data-action-onchange="xxx"` 可在 change 时自动触发动作（下拉联动用，别再加手动切换按钮）**；联动切换在途自动锁定面板内 select（失败/异常自动恢复）；每次动作透传 `root`（工作区路径）与 `session`（当前会话 ID）。
- **面板请求有序号防护（竞态修复，实测踩过）**：`state` 跨 RPC 是 JSON 深拷贝，两个在途请求各自基于同一旧 state 分叉；慢响应（冷缓存的 `llm.listModels` 可达秒级）后到会把新响应的 html/state 覆盖回旧值——用户连切 provider 时最终显示与实际路由错乱。Client 壳 `loadPanel` 按 toolId 发序号、只应用最新响应，过期响应直接丢弃。
- **state 必须纯 JSON 且轻量**。事件/响应等大本体每次动作重读服务或留闭包，别塞进 state（state 每次请求来回传输）。参考实现：`commitmsg`（diff 本体闭包）、`http`（响应体可达 256KB 闭包）、`git`（diff 文本闭包）、`compare`（多模型回答闭包）；重跑后闭包即空，面板按占位/重发提示优雅降级，旧 state 里的大字段进 handler 先 `delete` 迁移。

## 3. 真实坑清单（本机实测）

1. **共享类名写错 = 静默裸奔**。框架样式只认 `tb-*` 类；写成 `tb-query-input`（正确是 `tb-input`）时输入框直接回落宿主全局样式，无任何报错。写完先 grep 类名是否在 toolbox-client.js 共享层存在。
2. **子进程脚本的双重求值转义**。模板字符串里的 `\n` 在外层求值时已变真实换行，子脚本得到未终止字符串 → `SyntaxError`。**根治：数组 join('\n') 或不写字面换行**（见 plugins/http/tool.js）。验证：`eval` 模板 + `new Function(child)` 编译检查。
3. **pwsh 脚本严禁 stdin 喂多行**：`pwsh -Command -` 从 stdin 读脚本是**按行执行**，跨行块（`@(... ForEach-Object {`）静默失败无输出。`-EncodedCommand` 又要 base64 而插件求值器没有 Buffer——**首选 node 子进程**（`node -` 整脚本 stdin，process/Buffer/require 齐全；netstat/tasklist 用 spawnSync 解析，见 plugins/ports/tool.js）。
4. **Get-NetTCPConnection 在受限环境「拒绝访问」**；列端口用 `netstat -ano -p tcp` 解析（无需特权）。JS 字符串里的正则反斜杠记得双写：`"\\s+"` → ps1 得到 `'\s+'`。
5. **idPrefix 限 3–6 个小写英文字母**：`context`（7 个）被 cordis_define 拒绝，改 `contx`。
6. **全局 busy 横幅会打断操作**：每次点击都插「处理中…」导致面板抖动丢焦点。改按工具记录 `busyTool`，忙碌指示放 **Tab 上的小转圈**（CSS `@keyframes` 旋转），面板内容保持不动直到新 HTML 到达。
7. **单选改多选要做 state 迁移**：旧 state 字段（filter 字符串）在新代码里检测并映射成数组（filters），否则老面板状态直接报错或表现异常。
8. **Node 特有全局在 Host 动态代码里不可用**（实测：`Buffer is not defined`——求值器遮蔽 Buffer/process）。**但 `btoa`/`atob`/`TextEncoder`/`TextDecoder` 是 Host Builtin（btoa 直接处理 UTF-8 文本）**——base64 优先用内建 btoa/atob，`shared/host.js` 的纯 JS 实现留作兜底；web 标准全局（AbortController 等）不保证有，用 `typeof X !== 'undefined'` 守卫。**重活放子进程**：`node -`/`node -e` 起的是真实 node 进程，process/Buffer/require 都正常。
9. **Client 端官方无 document/window**：动态 Client 半经 `new Function` 求值，`document` 实际可达。rc.7 侧边栏**只有底部 footer 有官方 Slot**（`sidebar.footer.action`，root-scoped list，owner 只传 `{ wide }`）——实测位置太差（设置上方一行，远离导航热区），**用户决策：工具箱入口回导航区 DOM 注入**（新会话下方、taskboard/ssh 插件族块末尾 + MutationObserver 自愈），footer Slot 仅作无 DOM 环境兜底。规矩不变：DOM 注入**必须**配 `typeof document !== 'undefined'` 守卫 + 官方槽位兜底 + disposer 清理（停止即移除条目、断开 watcher）。
10. **服务契约以源码为准**：Inspect Provider 的 `referencedTypes` 常为空；去 `packages/extensions/tool-cordis/src/api-catalog.ts` 搜 `declaration: 'export interface XXX'` 拿权威形状（SessionEvent/ToolResultBlock/TokenUsage/GenerateOptions/StreamChunk/PromptAssembly/ToolSchema 都是这么确认的）。
11. **fs.writeText 不传策略 = 静默写不进工作区**（记录持久化头号坑）。jira 记录曾因此昨天落盘、今天全丢：`sandboxPolicy` 缺省回落 `resolve()`（无会话），其可写根是**宿主进程 cwd** 而非会话工作区 → `FS_SANDBOX_DENIED` → catch 里 console 一句就完事，UI 照常显示。**规矩：写工作区必须显式传策略**——有会话 `sp.resolve({ session })`，无会话 `{ mode: 'workspace-write', workspaceRoot: wsRoot }`；且持久化失败要在面板出警告，不许静默。
12. **`sessionQuery.readSession` 是全量重活**（读+解压+解析+回放校验+深克隆整条日志，含逐 token 的 assistant/chunk，十几 MB 级）——每次面板动作都调 = 点一下卡一下。**规矩：读会话日志一律走 `shared/host.js` 的 `makeSessionLogReader`**（活会话内存快照零 IO、持久化会话增量 readFrom、按 (sid,count) 命中），上层再缓存自己的构建结果（轨迹/用量都这么干）。
13. **git porcelain 两个坑（git 工具实测）**：① 默认 `status --porcelain` 把非 ASCII 路径转义成 `"八进制"` 形式（core.quotepath），显示难看且不能直接当 pathspec——**一律加 `-z`**（NUL 分隔、零转义；R/C 条目下一条记录是源路径，跳过即可）。② porcelain/numstat 的路径都**相对仓库顶层**，工作区根是 repo 子目录时 `git diff -- <path>` 会静默对不上——path 类命令先把 cwd 换成 `rev-parse --show-toplevel` 的结果。另外未跟踪新文件的 diff 用 `git diff --no-index -- /dev/null <path>`（有差异 exit 1 属正常）。
14. **bootstrapper registry.panel session 丢弃（v6.5 根因，重启后生效）**。`host-bootstrap/index.js` 的 `makeRegistry().panel()` 有 typo：`call.session === 'string'`（永远 false，应为 `typeof call.session === 'string' && call.session`），导致 `toolboxRegistry` 是进程级全局服务名，框架 `ctx.get('toolboxRegistry')` 拿到的就是这份有 bug 的注册表——所有面板调用的 `session`（当前会话 ID）在 Host 侧被永久丢弃，会话类工具（搜索/谱系/流镜）收到空 session → "未找到当前会话"。**诊断方法**：动态插件 Host 半 `ctx.get('toolboxRegistry').panel(undefined, { tool:'search', session:'TEST' })`，若返回 html 含 "未找到当前会话" 即确认。**修复**：改 `host-bootstrap/index.js`（junction 指向工作区，改源码即改运行代码，但静态插件需重启 DSH 生效）+ `host.js` 新增 `toolbox/session-info` RPC（按 sessionId 查会话 header.cwd）。
15. **宿主会话视角 useSessions 不响应浏览器 UI 切会话（v6.5 伴生问题）**。抽屉主实例挂在 `toolbox-host-*` 垫片会话下，其 Client 半渲染在宿主会话的 app-shell host 里——宿主 host 的 `useSessions` 拿不到浏览器 UI 当前会话（hook 为空/固定），切会话不跟随。**事实**：app-shell 每次切会话写 `localStorage['dsh.sessions.current']`（浏览器全局权威），抽屉经 `patch Storage.prototype.setItem` + 自定义事件 `tb-session-changed` 监听，切会话**零延迟**响应（事件桥）。`useSessions` hook 作响应式兜底（宿主 host 异常时无值但不报错）。**轮询已移除**（v6.5.1），跟随全靠事件桥 + useState 初始读取。

## 4. 本机已验证可用的关键服务（Host）

| 服务 | 用途 | 关键方法 |
| --- | --- | --- |
| `fs` | 文件读写（桩加载依赖） | resolve/stat/readText/listDir/writeText |
| `subprocess` | 子进程 | spawn({argv, stdio:{stdin:{data},stdout:{maxBytes}}, graceMs, env}) → handle.done / handle.collected |
| `sessionQuery` | 会话日志 | readSession(id) 全量事件 / readSurface(id) 当前上下文 / listSessions() 最新在前 / listEvents/readEvent |
| `systemPrompt` | 提示词装配 | assemble({}) → {sections[], contexts[], tools[], variables{}}（空 context 无会话级内容） |
| `llm` | 直调模型 | stream({provider, model, messages, signal}) 异步迭代 StreamChunk（text-delta/usage/finish） |
| `agentDefaultModel` | 当前模型路由 | currentSelection() → {provider, model} |
| `tokenMeter` | token 估算 | estimateMessage(message) |
| `tools` | 工具注册表 | schemas() → [{name, description, parameters}] |
| `credentials` | 凭据 | resolve/describe/set/unset（三源读：环境变量 / ~/.dsh/.credentials.yaml / 项目 .env；set/unset 写服务可写层 = 与 Harness 设置的 API Key 同存储，Jira 面板「凭据设置」即此；describe 只显状态不泄密，set 拒绝空值、被只读来源遮蔽时拒绝） |
| `timer` | 定时器 | ctx.timeout/ctx.interval（**禁用原生 setTimeout**） |

会话事件骨架（core/session/src/types.ts）：`tool/call {turn,step,callId,name,arguments(原始 JSON 字符串)}` ↔ `tool/result {message.content[0].toolCallId, error?, meta?}` 配对；`assistant/message` 带 `usage {inputTokens, outputTokens, cacheReadTokens...}`；`user/message.source.kind` 区分 用户/插件注入。

## 5. Client 半规则

- 只认五个 Builtin：`ctx / React / host / styles / console`；**React 用 createElement，禁 JSX**；UI 必须注册进查询过的 Slot（先 `Slots.listSubTree` 看树再注册）。
- 工具箱 Client 加载桩另显式下传 `setTimeout/setInterval/clearTimeout/clearInterval` 四个适配器（rc.7 改造 16.3）：内部是 Cordis timer 服务的数字句柄映射，行为同浏览器（setTimeout 返回 id、clear 取消），Package 停止/重跑时未决回调随 fiber teardown 全清——client.js 里照常用，不要再想办法读浏览器全局。
- `styles.insert(css)` 返回 disposer，配 `ctx.effect(() => dispose)`；主题插件只声明 `--tb-*` 变量（框架永不声明、只消费），停止即回默认。
- 面板 busy 指示放 Tab 转圈；`dangerouslySetInnerHTML` 的内容必须 Host 侧 `esc()` 转义用户数据。
- **分栏布局（固定头 + 独立滚动体）是标准面板模式**：根 `<div class="jr-tabpanel tb-root tb-pane">` → 固定区 `.tb-pane-head`（查询行/统计/详情卡）+ 滚动区 `.tb-pane-body`。要点：
  - 高度链靠 `:has(.tb-pane)` 作用域化（`jr-drawer-body`/`tb-frame` 只在含 pane 时切换 overflow），**不影响整页滚动的其他工具**；
  - `.tb-pane-body` 默认 `column-reverse`——DOM 最新在前 ⇒ 视觉越下越新、滚动条默认停底部（轨迹/时间线类用，零 JS）；
  - 普通列表用 `.tb-pane-body tb-pane-col`（正常方向），上下文/提示词/Jira 都是这个；
  - 详情卡放固定头（内容本身有 max-height 兜底），列表/记录进滚动体。

## 6. 工作流（每次开发按此走）

1. `node make-payloads.mjs`：语法检查全部 impl + 重新生成 payload（TOOOLS 数组加行即扩新工具）。
2. 子进程脚本改动 → 写冒烟脚本验证（提取数组 → join → `new Function` 编译；pwsh 脚本 `-EncodedCommand` 实跑只读命令）。
3. 新插件：`cordis_define`（payload 逐字传入 code）→ `cordis_run`；Host-only 免批准直接 running。已有插件改代码：抽屉齿轮点「重跑」（或 `cordis_run` 重跑），桩重读磁盘，永不重新 define。
4. 验证：`cordis_inspect_self(pluginId, packageId)` 看 runtime.state / waitingFor / 诊断；框架改动记得重跑 tbx。
5. 文档三件套同步：`plugins.json`（order/autoStart/approval）、`REBUILD.md`（重建表/文件表/数量）、`PLUGIN-DEV.md`（新坑入库）。

## 7. 数据与安全红线

- **凡持有记录/历史的工具，一律持久化到工作区 `<cwd>/.dsh-dynamic-toolbox/`**（面板 state 只做镜像，开 Tab 时磁盘为准恢复）。共享助手在 `shared/host.js`：`readJsonStore` / `writeJsonStore` / `storePolicy` / `ensureStoreDir` / `resolveWorkspace` / `makeLlmHelper`（AI 工具路由+调用）。现有映射：
  - `.dsh-dynamic-toolbox/jira-watch.json` — Jira 查询记录
  - `.dsh-dynamic-toolbox/toolbox-http.json` — HTTP 历史（8 条完整请求快照）
  - `.dsh-dynamic-toolbox/toolbox-ask.json` — 问答历史（10 条）
  - `.dsh-dynamic-toolbox/toolbox-compare.json` — 多模型对比（最近 3 轮，回答各截 4000 字符）
  - `.dsh-dynamic-toolbox/toolbox-translate.json` / `toolbox-promptopt.json` / `toolbox-commitmsg.json` / `toolbox-review.json` / `toolbox-aisummary.json` — AI 工具记录
  - `.dsh-dynamic-toolbox/toolbox-plugins.json` — 插件启停记忆（重建默认恢复上次开关；齿轮开关/重跑/批量自动写）
  - `.dsh-dynamic-toolbox/toolbox-ai-usage.json` — AI 工具用量台账（makeLlmHelper track 追加，cap 100；各工具路由行显示本工具累计次数/输出 token）
  - `.dsh-dynamic-toolbox/toolbox-search.json` — 会话搜索的最近 8 条查询（点芯片重搜）
  - `.dsh-dynamic-toolbox/toolbox-autorebuild.json` — 框架自举报告（分阶段，含 ms 耗时；停在哪阶段问题就在哪之后）
  - `Jira-Issue/<KEY>/` — 附件归档
- 不把活对象（Service/Session/事件）JSON.stringify 或整体进 state；只提取需要的标量叶子。
- 凭据走 `credentials` 服务或子进程环境变量；用户输入**绝不**拼进子进程脚本文本（走 env / stdin data）。
- 破坏性动作两步确认（如端口工具的「结束进程」先武装再确认）。
- 直调 LLM 的面板（问答/对比/翻译/提示优化/提交信息/评审/摘要）都消耗真实 API 额度，purpose 与面板上要标注。
